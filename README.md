# MacOS Convenient Shell Script

`shell
# start using
launchctl load com.halfkai.example.plist

# stop using
launchctl unload com.halfkai.example.plist
`

---------------

Note: You have to do a little twig before using launchctl